#ifndef PLAYBACKVIEW_H
#define PLAYBACKVIEW_H

#include "MediaFile.h"
#include "Playlist.h"
#include <vector>
#include <string>
#include <map>
#include <memory>
#include <iostream>


class PlaybackView {
public:
    // void displayPlaybackMenu();

    // void displayCurrentFile(MediaFile* file);

    // void displayStatus(const std::string& status);

    // void playPlaylist(std::shared_ptr<Playlist> playlist);
    char choicePlayback();
};

#endif // PLAYBACKVIEW_H
